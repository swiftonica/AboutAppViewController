import XCTest
@testable import AboutAppViewController

final class AboutAppViewControllerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AboutAppViewController().text, "Hello, World!")
    }
}
